<% if model.errors.any? %>
  <h2><%= model.class %> couldn't be saved because of the fallowing
  <%= "error".pluralize(model.errors.count) %>:</h2>
  <ul>
    <% model.errors.each do |k, v| %>
      <li> <%= v.capitalize %> </li>
    <% end %>
  </ul>

<% end %>
