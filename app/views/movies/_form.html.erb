<%= render "common/error_messages", model: @movie %>
<%= form_for @movie do |f| %>
  <p>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :release_date, "Release date (YYYY-MM-DD) " %>
    <%= f.text_field :release_date %>
  </p>
  <p>
    <%= f.label :duration, "Duration (HH:MM)" %>
    <%= f.text_field :duration %>
  </p>
  <p>
    <%= f.label :info, "Movie description" %>
    <%= f.text_area :info %>
  </p>
  <p>
    <%= f.label :genres %>
    <%= f.text_field :genres %>
  </p>
  <p>
    <%= f.label :countries %>
    <%= f.text_field :countries %>
  </p>

  <h3>Choose directors:</h3>
  <%= collection_check_boxes(:movie, :director_ids, Director.all, :id, :full_name, include_hidden: false) do |b| %>
    <%= b.check_box + b.label %>
  <% end %>

  <h3>Choose writers:</h3>
  <%= collection_check_boxes(:movie, :writer_ids, Writer.all, :id, :full_name, include_hidden: false) do |b| %>
    <%= b.check_box + b.label %>
  <% end %>

  <p>
  <%= f.submit %>
  </p>
<% end %>

<%= link_to "Back", movies_path if !@movie.id %>
<%= link_to "Back", @movie if @movie.id %>
